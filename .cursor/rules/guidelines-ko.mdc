---
# ⚠️ 이 파일은 AI가 읽지 않습니다 (Human Reference Only)
# AI는 guidelines.mdc (영문판)만 참조합니다
---

# AGROUNDS 개발 빠른 참조 & 네비게이션 가이드

## 🎯 역할 정의
당신은 AGROUNDS 개발 코디네이터입니다. 이 가이드를 사용하여 프로젝트 제약사항을 빠르게 이해하고 개발 작업에 따라 적절한 상세 가이드라인으로 이동하세요.

## 📌 참고사항
- 이 파일은 **사람 개발자를 위한 한글 참조용**입니다
- AI는 영문판 파일만 읽습니다 (`guidelines.mdc`)
- 규칙 변경시 영문판과 한글판 모두 업데이트해야 합니다

## 🚨 보편적 제약사항 - 예외 없음

### ❌ 절대 금지 사항
1. **새 파일 생성 금지** - 기존 파일만 수정
2. **하드 삭제 금지** - 항상 `deleted_at` 필드로 soft delete 구현  
3. **디자인 시스템 위반 금지** - 확립된 CSS 변수를 엄격히 준수
4. **관련 없는 코드 변경 금지** - 요청과 직접 관련된 코드만 수정

### ✅ 핵심 요구사항
1. **SOFT DELETE 패턴** - `deleted_at` 타임스탬프 필드 사용
2. **기존 패턴** - 확립된 코드 규칙을 정확히 따름
3. **프로덕션 테스트** - `https://agrounds.com/`에서 모든 변경사항 테스트
4. **디자인 일관성** - 승인된 색상, 타이포그래피, 간격 사용

## 📋 작업 기반 네비게이션 가이드

### 🗄️ **데이터베이스 접근 & 검증** ⭐ 최우선
**언제 사용**: DB 스키마 확인, models.py와 실제 DB 동기화, MySQL 접근
**상세 가이드**: `database/access.mdc` → `database/patterns.mdc` 참조
**핵심 포커스**: 실제 DB 스키마 검증, enum 매핑, nullable 체크
**예시**: "DB와 models.py 일치 확인", "user 테이블 실제 구조 확인"

### 🔧 **Backend API 개발**
**언제 사용**: Django REST API 엔드포인트 생성/수정, 데이터베이스 작업
**상세 가이드**: `backend/api-development.mdc` 참조
**핵심 포커스**: Django views, serializers, soft delete, JWT 인증

### 🎨 **Frontend React 개발** 
**언제 사용**: React 컴포넌트 구축/수정, 사용자 인터페이스
**상세 가이드**: `frontend/components.mdc` 참조
**핵심 포커스**: 컴포넌트 패턴, 상태 관리, 반응형 디자인

### 🎭 **UI/UX & 스타일링 작업**
**언제 사용**: CSS/SCSS 작업, 디자인 시스템 컴포넌트
**상세 가이드**: `frontend/design-system.mdc` 참조
**핵심 포커스**: CSS 변수, 타이포그래피 클래스, 접근성 표준

### 🗄️ **데이터베이스 패턴 & 모델**
**언제 사용**: Django 모델 작업, 데이터베이스 쿼리, 데이터 무결성
**상세 가이드**: `database/patterns.mdc` 참조
**핵심 포커스**: 모델 구조, soft delete, 쿼리 최적화

### 🏗️ **일반 프로젝트 아키텍처**
**언제 사용**: 전체 프로젝트 구조 및 제약사항 이해
**상세 가이드**: `general/project-guidelines.mdc` 참조
**핵심 포커스**: 프로젝트 개요, 파일 구성, 개발 워크플로우

## ⚡ 빠른 의사결정 매트릭스

| 작업 유형 | 주 규칙 파일 | 보조 파일 |
|-----------|------------|----------|
| **DB 스키마 확인** | `database/access.mdc` | `database/patterns.mdc` |
| API 엔드포인트 | `backend/api-development.mdc` | `database/patterns.mdc` |
| React 컴포넌트 | `frontend/components.mdc` | `frontend/design-system.mdc` |
| CSS 스타일링 | `frontend/design-system.mdc` | `frontend/components.mdc` |
| 데이터베이스 쿼리 | `database/patterns.mdc` | `backend/api-development.mdc` |
| 프로젝트 설정 | `general/project-guidelines.mdc` | 모두 |

## 🔥 중요 성공 요소

### 모든 작업 시작 전:
1. **작업 유형 식별** - 위 네비게이션 가이드 사용
2. **특정 규칙 파일 읽기** - 상세 가이드라인 건너뛰지 말 것
3. **제약사항 확인** - 보편적 금지사항 체크
4. **한계 내에서 계획** - 기존 파일로만 작업
5. **프로덕션 테스트** - `https://agrounds.com/`에서 검증

### 개발 워크플로우:
```
1. 네비게이트 → 적절한 상세 규칙 파일 선택
2. 분석 → 기존 코드 패턴 학습  
3. 계획 → 제약사항 내에서 변경 설계
4. 구현 → 특정 가이드라인 정확히 따름
5. 테스트 → 프로덕션 환경에서 검증
6. 검증 → 디자인 시스템 준수 확인
```

## 🎯 문제 해결 접근법

### 단계별 프로세스:
1. **분류** - 네비게이션 가이드로 작업 분류
2. **참조** - 적절한 상세 규칙 파일 확인
3. **분석** - 코드베이스에서 유사한 기존 구현 분석
4. **따르기** - 확립된 패턴 정확히 따름
5. **구현** - 적절한 에러 핸들링 포함
6. **검증** - 모든 적용 가능한 규칙 파일에 대해 검증

## ⚠️ 긴급 제약사항 알림

**아무것도 기억 못 해도 이것만 기억하세요:**
- ❌ **새 파일 금지** - 기존 파일만 수정
- ❌ **하드 삭제 금지** - `deleted_at` 필드 사용
- ❌ **색상 하드코딩 금지** - CSS 변수 사용
- ✅ **프로덕션 테스트** - `https://agrounds.com/`

## 🔗 상세 가이드 빠른 링크

### 📊 데이터베이스
- 🔍 **[데이터베이스 접근 & 검증](database/access.mdc)** ⭐ **실제 DB 접근 방법**
- 📕 **[데이터베이스 패턴](database/patterns.mdc)** - 모델 & 쿼리 가이드라인

### 💻 백엔드
- 📘 **[백엔드 개발 규칙](backend/api-development.mdc)** - Django REST Framework 패턴

### 🎨 프론트엔드  
- 📗 **[프론트엔드 개발 규칙](frontend/components.mdc)** - React 컴포넌트 패턴
- 📙 **[디자인 시스템 규칙](frontend/design-system.mdc)** - UI/UX 표준

### 🏗️ 일반
- 📓 **[프로젝트 가이드라인](general/project-guidelines.mdc)** - 전체 아키텍처
- 🧭 **[네비게이션 허브](00-navigation-hub.mdc)** - 전체 규칙 네비게이션

**기억하세요**: 이것은 네비게이션 허브입니다. 개발 작업을 시작하기 전에 항상 작업 유형에 맞는 특정 상세 규칙 파일을 참조하세요.
